<script type="text/javascript">
  // Load the Visualization API and the corechart package.
  google.charts.load('current', {'packages':['corechart']});
  // Set a callback to run when the Google Visualization API is loaded.
  google.charts.setOnLoadCallback(drawChart_sales_by_SKU);

  // Callback that creates and populates a data table,
  // instantiates the pie chart, passes in the data and
  // draws it.
  function drawChart_sales_by_SKU() {

    // Create the data table.
    var data = new google.visualization.DataTable();
    data.addColumn('string', 'Topping');
    data.addColumn('number', 'Slices');
    // data.addRows([
    //   ['Boots', 34],
    //   ['Pumps', 23],
    //   ['Pumps Laces', 14]
    // ]);
    data.addRows(<%= raw @sales_by_SKU.map.to_json %>);
    var title = <%= raw @sales_by_SKU_title.to_json %>

    // Set chart options
    var options = {'title':title};

    // Instantiate and draw our chart, passing in some options.
    var chart = new google.visualization.PieChart(document.getElementById('chart_sales_by_SKU'));
    chart.draw(data, options);
  }
</script>