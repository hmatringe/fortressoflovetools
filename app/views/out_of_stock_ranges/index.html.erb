<%= render partial: "shared/subnavigation", locals: { current: :out_of_stock_ranges} %>
<%= render partial: "form" %>

<div class="container">
  <div class="row">
    <div class="col-xs-12">
      <h1>Out of stocks</h1>
      <table class="table table-striped">
        <tr>
          <th>SKU</th>
          <th>EAN</th>
          <th>name</th>
          <th>Date Range</th>
          <th>Number of days</th>
          <th>Actions</th>
        </tr>
        <% @out_of_stock_ranges.each do |ofsr| %>
          <tr>
            <td><%= link_to ofsr.product.SKU, product_path(ofsr.product) %></td>
            <td><%= link_to ofsr.product.EAN, product_path(ofsr.product) %></td>
            <td><%= link_to ofsr.product.short_name, product_path(ofsr.product) %></td>
            <td><%= ofsr.date_range %></td>
            <td><%= ofsr.days %></td>
            <td><%= link_to "delete", out_of_stock_range_path(ofsr), method: :delete %></td>
            <!-- <td> -->
        <%#= link_to "Edit", edit_product_path(product), class: "cogs_btn" %>
      <!-- </td> -->
          </tr>
        <% end %>
      </table>
    </div>
  </div>
</div>